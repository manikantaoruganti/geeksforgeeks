public class Solution {
    public void utility(Scanner scn) {
        String a = scn.next();
        String b = scn.next();

        // Print 'a', followed by a space, then 'b', and finally a new line.
        System.out.println(a + " " + b);
    }
}