#include <stdio.h>
int main() {
    int x, y;
    scanf("%d %d", &x, &y);
    
    // code here
     
    // perform addition
    int p = x + y;
    // perform Subtraction
    int q = x - y;
    // perform Multiplication
    int r = x * y;
    // perform Division 
    int s = x / y;
    // perform Modulo
    int t = x % y;
    
    printf("%d %d %d %d %d", p, q, r, s, t);
    
    return 0;
} 


